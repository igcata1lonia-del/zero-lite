export const metadata = {
  title: 'Zero Lite',
  description: 'Unified Email Client',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
